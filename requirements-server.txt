# Server requirements - only what's needed to run the Flask app
# This excludes data processing dependencies that only run locally

# Web framework
Flask==2.3.3
Flask-CORS==4.0.0
Flask-Compress==1.14
Werkzeug==2.3.7
gunicorn==21.2.0

# Utilities
python-dotenv==1.0.0
requests==2.31.0

# Data handling (used by app/data_loader.py and app/province_loader.py)
numpy>=1.26.0
rasterio>=1.3.0
geopandas>=0.14.0

# Note: pandas, shapely, pyproj are dependencies of geopandas and will be installed automatically
