# Development requirements - includes all dependencies for data processing scripts
# Use this for local development when running the full pipeline

# Include server requirements
-r requirements-server.txt

# Data processing (only needed for scripts/, not for Flask app)
pandas>=2.1.0
Pillow>=10.1.0
rasterstats>=0.19.0
shapely>=2.0.0
pyproj>=3.6.0
xarray>=2023.0.0
netCDF4>=1.6.0
cfgrib>=0.9.10
cdsapi>=0.6.1topojson>=1.5  # For GeoJSON to TopoJSON conversion