# Main requirements file - USE requirements-server.txt for production deployment
# This file kept for backwards compatibility but is now deprecated
# 
# For production server: pip install -r requirements-server.txt
# For local development: pip install -r requirements-dev.txt

-r requirements-server.txt
